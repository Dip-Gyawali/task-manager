import React from 'react'
import { useParams } from 'react-router-dom'

export default function UpdateTask() {
    const {id} = useParams();
    
    
  return (
    <div>
      update section
    </div>
  )
}
